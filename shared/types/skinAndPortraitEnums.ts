enum PortraitEnum {
    DEFAULT = 'default',
    DEFAULT_MALE = 'default_male',
    DEFAULT_FEMALE = 'default_female',
}

enum CardBackEnum {
    DEFAULT = 'default',
}

enum InfantrySkinEnum {
    DEFAULT = 'default',
}

enum CavalrySkinEnum {
    DEFAULT = 'default',
}

enum ArcherSkinEnum {
    DEFAULT = 'default',
}

enum MageSkinEnum {
    DEFAULT = 'default',
}

enum ClericSkinEnum {
    DEFAULT = 'default',
}

enum SiegeUnitSkinEnum {
    DEFAULT = 'default',
}

export {
    PortraitEnum,
    CardBackEnum,
    InfantrySkinEnum,
    CavalrySkinEnum,
    ArcherSkinEnum,
    MageSkinEnum,
    ClericSkinEnum,
    SiegeUnitSkinEnum,
}
